{
  "name": "alechales-portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "Alec Hales portfolio — static site deployed to Cloudflare Workers static assets.",
  "scripts": {
    "deploy": "wrangler deploy",
    "preview": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
